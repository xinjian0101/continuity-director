# Project Lock / 项目锁

Creates a stable project record containing title, aspect ratio, FPS, interface language, and production notes.

创建包含标题、画幅、帧率、界面语言和制作备注的稳定项目记录。

The language field is production metadata; changing the dashboard language does not rewrite workflow data.
