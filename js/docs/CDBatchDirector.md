# Batch Director / 批量导演

Expands storyboard JSON into deterministic shot and Take variants.

将分镜 JSON 扩展为确定性的镜头和多个 Take。

Each Take receives a stable ID and seed derived from the base seed unless a shot seed is supplied.
