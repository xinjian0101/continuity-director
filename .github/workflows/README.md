# Workflows

`ci.yml` validates the plugin on Python 3.10, 3.11, and 3.12. Legacy source-reconstruction workflows were removed after the complete plugin source was committed directly to the repository.
