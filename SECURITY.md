# Security Policy

本插件 v0.2.0：

- 不直接调用外部视频平台。
- 不读取、保存或上传 API Key。
- 本地参考图只接受安全相对路径，拒绝绝对路径和 `..` 路径穿越。
- 输出文件名经过安全化处理，并使用原子替换写入。
- 项目 ZIP 使用固定内部路径，不从用户输入写入任意归档路径。

请通过 GitHub Security Advisory 私下报告路径穿越、任意文件写入、密钥泄露或代码执行问题，不要先公开 Issue。
