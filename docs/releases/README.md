# Release Notes

Each public version has a dedicated release-note file named `v<version>.md`. The automatic publication workflow reads the file matching `VERSION`, validates the repository, builds the installable ZIP, generates the SHA-256 checksum, and publishes a GitHub prerelease.
